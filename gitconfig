# vim: syntax=gitconfig
# -----------------------------------------------------------------------------
# File: gitconfig
# Info: This is Git's per-user configuration file
# -----------------------------------------------------------------------------

[alias]
  added = difftool --cached
  aliases = config --get-regexp alias
  br = branch
  branches = branch -a
  co = checkout
  create = !git init && git add -a && git commit -m \"Initial Commit\"
  do = "!f() { git add $1 && git commit -m \"$2\" && git push; }; f"
  remotes = remote -v
  showtool = "!f() { git difftool ${1:-HEAD}^ ${1:-HEAD};}; f"
  st = status -sb
  tags = tag -l
  up = pull
  who = shortlog -s --

[diff]
  tool = icdiff

[difftool]
  prompt = false

[difftool "icdiff"]
  cmd = /usr/local/bin/icdiff --line-numbers $LOCAL $REMOTE

[pager]
  difftool = true

[user]
  name = "Andrew Kuttor"
  email = "andrew.kuttor@gmail.com"

[advice]
  pushNonFastForward = false
  statusHints = false

[apply]

  whitespace = fix

[core]

  whitespace = space-before-tab,-indent-with-non-tab,trailing-space
  excludesfile = $HOME/.gitignore
  attributesfile = $HOME/.gitattributes
  trustctime = false
  untrackedCache = true

[help]

  autocorrect = 1

[diff]

  renames = copies
  mnemonicprefix = true

[push]
  default = matching

[merge]
  stat = true
  merge = true

[rerere]
  enabled = true
  autoupdate = 1

[credential]
  helper = osxkeychain

[color]
  ui = auto

# -----------------------------------------------------------------------------
# Git Repo Servers
# -----------------------------------------------------------------------------

# GitHub
[url "https://github.com/"]
  insteadOf = gh:

# BitBucket
[url "ssh://git@bitbucket.mcp-services.net:7999/"]
    insteadOf = bb:

[url "https://gist.github.com/"]
    insteadOf = gist:
